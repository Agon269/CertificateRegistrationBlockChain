const Certificate = artifacts.require("Certificate");

contract("Certificate", (acciounts) => {
  it("Should...", async () => {
    const certificateInstance = Certificate.deployed();
    const balance = certificateInstance;
    return 1 === 1;
  });
});
